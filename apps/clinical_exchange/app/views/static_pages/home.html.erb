<% content_for :title, "Clinical Exchange" %>

<%= render_header(title: "Clinical Exchange", show_search: false) %>

<main class="p-6">
  <%= render_card(title: "Welcome",
        description: "Share clinical records between care settings.") do %>
    <p>
      There is nothing here yet. This page confirms the shared design system is
      wired in.
    </p>
  <% end %>
</main>
