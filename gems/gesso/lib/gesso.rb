# The component partials use inline_svg_tag; require it here so consuming
# apps get the helper without listing inline_svg in their own Gemfile.
require "inline_svg"

require "gesso/version"
require "gesso/engine"

module Gesso
end
