require "bundler/setup"
require "bundler/gem_tasks"

# Gem-local tasks (lint, …), kept out of lib/ so nothing leaks into
# consuming apps.
Dir.glob(File.expand_path("tasks/*.rake", __dir__)).each { |task| load task }
